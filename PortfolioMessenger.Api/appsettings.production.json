{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "AllowedOrigin": "https://briankiariemwaniki.dev",
  "EmailApiAccessOptions": {
    "ServiceId": "${SERVICE_ID}",
    "TemplateId": "${TEMPLATE_ID}",
    "UserId": "${USER_ID}",
    "AccessToken": "${ACCESS_TOKEN}"
  }
}